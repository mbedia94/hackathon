{
  "name": "root",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "types"
  ],
  "scripts": {
    "start:frontend": "dotenv -- turbo run dev --scope='frontend'  --include-dependencies --no-deps",
    "start:backend": "dotenv -- turbo run dev --scope='backend'  --include-dependencies --no-deps"
  },
  "devDependencies": {
    "@types/node": "^18.14.1",
    "dotenv-cli": "^7.0.0",
    "ts-node": "^10.9.1",
    "turbo": "^1.8.2",
    "typescript": "^4.9.5"
  }
}
